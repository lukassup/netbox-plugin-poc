from setuptools import find_packages, setup

setup(
    name='netbox-provisioning',
    version='0.1.0',
    description='Netbox provisioning plugin',
    install_requires=[],
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
)
